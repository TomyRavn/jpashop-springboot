spring:
# *** Spring Boot에서는 내용이 없으면 Default로 memory DB로 동작
#  datasource:
#    url: jdbc:h2:mem:test      #test에서는 memory DB를 사용하겠다는 뜻
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop    #create-drop은 종료 시 drop까지 처리
#    properties:
#      hibernate:
##        show_sql: true         (system.out)
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug    #log
    org.hibernate.type: trace